# Live E2E Record: 2026-07-18

- Command: `uv run python scripts/regression.py --live-e2e`
- Scope: isolated Markdown -> ingestion -> LLM Wiki -> learning path -> cited QA build
- Result: pass; the temporary work directory was removed by the success path
- Observed duration: about one minute
- Chat configuration: `deepseek` / `deepseek-chat`
- Embedding configuration: `dashscope` / `text-embedding-v4`
- Data isolation: temporary data directory only; no public Demo catalog or local `data/` project was used
- Cost estimate: not measured. The current provider adapters discard response usage metadata, so this record intentionally does not claim a dollar amount.

This live run is separate from the `25 / 25` golden-catalog score. The score is
deterministic and makes zero provider calls; the live run only confirms that the
configured provider-backed build path can still complete.
